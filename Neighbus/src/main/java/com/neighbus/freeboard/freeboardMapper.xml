<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.neighbus.freeboard.FreeboardMapper">
   
    <!-- 게시글 작성 (freeboards 테이블에 INSERT) -->
    <insert id="insertPost" parameterType="com.neighbus.freeboard.FreeboardDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO freeboards (
            title,
            content,
            writer,
            image_name
        ) VALUES (
            #{title},
            #{content},
            #{writer}, <!-- FreeboardDTO의 writer 필드는 users.id 값 (INT)이어야 함 -->
            #{imageName}
        )
    </insert>

    <!-- 게시글 목록 조회 (작성자 닉네임 포함) -->
    <select id="selectPostList" resultType="com.neighbus.freeboard.FreeboardDTO">
        SELECT 
            f.id,
            f.title,
            f.content,
            f.writer,
            f.image_name AS imageName,
            DATE_FORMAT(f.created_at, '%Y-%m-%d %H:%i') AS createdAt,
            f.view_count AS viewCount,
            u.nickname AS writerNickname <!-- DTO의 writerNickname 필드에 매핑 -->
        FROM freeboards f
        JOIN users u ON f.writer = u.id
        ORDER BY f.created_at DESC
    </select>

    <!-- 게시글 상세 조회 (작성자 닉네임 포함) -->
    <select id="selectPostDetail" parameterType="int" resultType="com.neighbus.freeboard.FreeboardDTO">
        SELECT 
            f.id,
            f.title,
            f.content,
            f.writer,
            f.image_name AS imageName,
            DATE_FORMAT(f.created_at, '%Y-%m-%d %H:%i:%s') AS createdAt,
            f.view_count AS viewCount,
            u.nickname AS writerNickname
        FROM freeboards f
        JOIN users u ON f.writer = u.id
        WHERE f.id = #{id}
    </select>
    
    <!-- 조회수 증가 -->
    <update id="incrementViewCount" parameterType="int">
        UPDATE freeboards
        SET view_count = view_count + 1
        WHERE id = #{id}
    </update>

</mapper>