<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.neighbus.gallery.GalleryDAO">

	<insert id="insertGallery" parameterType="com.neighbus.gallery.GalleryDTO">
		insert into gallery (
			title,
			content,
			writer
		) values (
			#{title},
			#{content},
			(select id from users where username=#{username})
		)
	</insert>
	
	<insert id="insertGalleryImage" parameterType="com.neighbus.gallery.GalleryDTO">
		insert into gallery_image (
			gallery_id,
			image_name
		) values 
		    <foreach collection="fileNameList" item="i" separator=",">
		        (#{galleryId}, #{i})
		    </foreach>
	</insert>
	
	<select id="getGalleryMaxId" parameterType="com.neighbus.gallery.GalleryDTO" resultType="int">
		select max(id) from gallery where writer=(select id from users where username=#{username})
	</select>
	
	
	
</mapper>